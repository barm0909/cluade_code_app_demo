// Cloudflare Workers (静的アセット配信) のデプロイ設定
// Workers Builds が `npm run build` 後に `npx wrangler deploy` でこれを読み、dist/ を配信する
{
  "name": "cluadecodeappdemo",
  "compatibility_date": "2026-07-05",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "single-page-application"
  }
}
